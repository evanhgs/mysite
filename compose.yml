services:
  web:
    image: mysite
    build:
      context: .
      args:
        NODE_VERSION: 24.12.0-alpine
        NGINX_VERSION: alpine3.22
    ports:
      - "7788:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
